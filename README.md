## Результаты тестирования моделей sentiment analysis

| Модель | Accuracy | Correct | Load time, sec | Total inference, sec | Avg inference, sec |
|---|---:|---:|---:|---:|---:|
| `nlptown/bert-base-multilingual-uncased-sentiment` | **1.0000** | **22/22** | 14.6622 | **0.0429** | **0.0019** |
| `distilbert/distilbert-base-uncased-finetuned-sst-2-english` | 0.7273 | 16/22 | **0.8141** | 0.1010 | 0.0046 |
| `cardiffnlp/twitter-roberta-base-sentiment-latest` | 0.5455 | 12/22 | 0.8220 | 0.0558 | 0.0025 |
| `ProsusAI/finbert` | 0.4545 | 10/22 | 0.8998 | 0.0477 | 0.0022 |

## Вывод

Лучшей моделью для задачи классификации тональности стала `nlptown/bert-base-multilingual-uncased-sentiment`.

Она показала accuracy **100%** на смешанном англо-русском тестовом наборе из **22 примеров**. Модель корректно обработала как англоязычные, так и русскоязычные отзывы.

Англоязычные модели `distilbert`, `finbert` и `twitter-roberta` показали снижение качества на русскоязычных текстах. Это подтверждает, что для мультиязычных сценариев необходимо выбирать модели, обученные на нескольких языках.

Главный недостаток `nlptown` — заметное время загрузки: **14.66 сек**. Поэтому в production-сценариях такую модель желательно держать прогретой в памяти.

## Итоговый выбор

Выбирается модель:

`nlptown/bert-base-multilingual-uncased-sentiment`

## Результаты тестирования моделей NER
| Модель                                    |     Recall |     Found | Load time, sec | Avg inference, sec |
| ----------------------------------------- | ---------: | --------: | -------------: | -----------------: |
| `Jean-Baptiste/roberta-large-ner-english` | **0.9600** | **24/25** |     **0.7470** |             0.0069 |
| `Davlan/xlm-roberta-base-ner-hrl`         |     0.9200 |     23/25 |         1.0197 |         **0.0025** |
| `dslim/bert-base-NER`                     |     0.6000 |     15/25 |         0.8760 |             0.0091 |
| `StanfordAIMI/stanford-deidentifier-base` |     0.4800 |     12/25 |         0.7953 |         **0.0025** |

### Вывод

Лучшее качество показала `Jean-Baptiste/roberta-large-ner-english` — **96% recall** на тестовом наборе из **10 текстов** и **25 сущностей**.

Для production выбран `Davlan/xlm-roberta-base-ner-hrl`, так как модель показала **92% recall**, более высокую скорость и более чистое извлечение сущностей на смешанных англо-русских текстах.

### Итоговый выбор

`Davlan/xlm-roberta-base-ner-hrl`

## Результаты тестирования модели ``DeepSeek Reasoner`

| Задача                   |  Метрика |      Score |    Result | Total inference, sec | Avg inference, sec | Tokens |
| ------------------------ | -------: | ---------: | --------: | -------------------: | -----------------: | -----: |
| Sentiment Analysis       | Accuracy | **1.0000** | **22/22** |              29.3142 |             1.3325 |   2797 |
| Named Entity Recognition |   Recall |    0.8800* |     22/25 |              16.0522 |             1.6052 |   1856 |

## Вывод

Модель `DeepSeek Reasoner` показала **100% accuracy** на задаче классификации тональности на смешанном англо-русском тестовом наборе из **22 примеров**.

В задаче NER модель показала **0.8800 recall** по строгой метрике exact match. При анализе результатов установлено, что часть ошибок связана не с пропуском сущностей, а с нормализацией словоформ (`Кремле → Кремль`, `Казани → Казань`, `Санкт-Петербурге → Санкт-Петербург`).

Фактически модель корректно извлекла все сущности, однако текущая метрика не учитывает семантическое совпадение.

## Итоговый вывод

`DeepSeek Reasoner` показал высокое качество в задачах sentiment analysis и semantic NER, однако уступает локальным transformer-моделям по скорости инференса. Модель целесообразно использовать для semantic extraction, аналитики и fallback-обработки сложных кейсов.
